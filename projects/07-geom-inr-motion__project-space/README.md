# geom-inr-motion

## Update (April 17, 2026)

- The tracked `geom-inr-motion/` source tree is not present in the current private `main`.
- Treat this project-space as a placeholder and documentation shell until the implementation is restored or re-imported.
- Only the project-space docs and Docker assets are currently available here.

## Overview
This project-space currently contains documentation and Docker assets for geom-inr-motion. The tracked implementation directory is absent from the current private `main`.

## Structure

```
geom-inr-motion__project-space/
├── README.md                 # This file
├── docker/                   # Docker definitions
│   ├── Dockerfile
│   └── scripts/
├── docs/                     # Research documentation
└── PROMPT_RUN_TESTS_RTX3090.md  # Historical remote validation prompt
```

## Quick Start

```bash
# Review the remaining tracked assets in this project-space
ls docs
ls docker
```

If the implementation tree is restored later, add the project-specific install and test steps back here.

## Related Documentation
See `docs/` directory for research notes and design documents.
